# opff
Open Pixmap File Format (OPFF) is a lightweight, uncompressed file format designed for memory-mapped graphic data, rapid algorithm debugging, and low-overhead multidimensional transfer. It strictly rejects semantic color assumptions, natively supporting both legacy object-based alignments and modern planar tensors.
